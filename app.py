# -*- coding: utf-8 -*-
"""app

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1imVO0vmJ0M9tTL2VgarmINrVW4FB13il
"""

import streamlit as st
import pandas as pd
import pickle
import time

# Load model
with open("final_model.pkl", "rb") as f:
    model = pickle.load(f)

# Page config
st.set_page_config(page_title="AI Visa Predictor", layout="centered")

# Custom CSS (🔥 UI MAGIC)
st.markdown("""
    <style>
    body {
        background-color: #0e1117;
        color: white;
    }
    .stApp {
        background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
    }
    .title {
        font-size: 40px;
        font-weight: bold;
        text-align: center;
        color: #00ffd5;
    }
    .card {
        background: rgba(255,255,255,0.1);
        padding: 20px;
        border-radius: 15px;
        box-shadow: 0px 0px 15px rgba(0,255,213,0.3);
    }
    </style>
""", unsafe_allow_html=True)

# Title
st.markdown('<p class="title">✈️ AI Visa Processing Predictor</p>', unsafe_allow_html=True)
st.write("Get smart predictions powered by Machine Learning 🚀")

# Card UI
with st.container():
    st.markdown('<div class="card">', unsafe_allow_html=True)

    col1, col2 = st.columns(2)

    with col1:
        country = st.text_input("🌍 Country", "India")
        visa_type = st.text_input("📄 Visa Type", "H1B")

    with col2:
        office = st.text_input("🏢 Processing Office", "CA")
        date = st.date_input("📅 Application Date")

    st.markdown('</div>', unsafe_allow_html=True)

# Predict button
if st.button("🚀 Predict Now"):

    with st.spinner("🤖 AI is analyzing your visa..."):
        time.sleep(2)

        input_data = pd.DataFrame([{
            "country": country,
            "visa_type": visa_type,
            "processing_office": office,
            "application_month": date.month,
            "country_avg": 100,
            "visa_avg": 100
        }])

        result = model.predict(input_data)[0]

    st.success(f"⏱ Estimated Time: {int(result)} days")

    st.progress(min(int(result), 100))

    st.info(f"📊 Expected Range: {int(result-5)} - {int(result+5)} days")

# Footer
st.markdown("---")
st.caption("⚡ Built with AI | Infosys Springboard Project")